# MapMates - Architecture Documentation

## 🏗️ System Architecture

### High-Level Overview

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Next.js 16 (React 19)                   │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │   Pages    │  │ Components │  │   Hooks    │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                      API Layer                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Supabase Client                         │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │    Auth    │  │  Database  │  │  Realtime  │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                    Backend Services                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                  Supabase                            │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │ PostgreSQL │  │  Auth API  │  │  Storage   │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                   External Services                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │Google Maps │  │   Vercel   │  │   CDN      │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

### Authentication Flow

```
User Input (Email/Password)
        ↓
AuthForm Component
        ↓
Supabase Auth API
        ↓
JWT Token Generated
        ↓
Session Stored (Cookie)
        ↓
Profile Created/Retrieved
        ↓
Redirect to Dashboard
```

### Map Interaction Flow

```
User Opens Dashboard
        ↓
Request Geolocation
        ↓
Get Current Position
        ↓
Update User Location in DB
        ↓
Fetch Nearby Users (1km radius)
        ↓
Calculate Distances
        ↓
Render Map with Markers
        ↓
User Clicks Marker
        ↓
Show Connect Modal
        ↓
Play Game or Skip
        ↓
Create Connection
        ↓
Unlock Chat
```

### Place Discovery Flow

```
User Browses Places
        ↓
Fetch Approved Places
        ↓
Apply Filters (Category, Budget)
        ↓
Display Place Cards
        ↓
User Clicks Place
        ↓
Show Place Modal
        ↓
Load Reviews & Details
        ↓
User Actions:
  - Like Place
  - Save to Wishlist
  - Get Directions
  - Write Review
```

### Mapos Transaction Flow

```
User Action (Game Win/Place Approval)
        ↓
Calculate Mapos Amount
        ↓
Create Transaction Record
        ↓
Update User Balance
        ↓
Show Celebration Animation
        ↓
Update UI
        ↓
Refresh Wallet
```

## 🗄️ Database Architecture

### Entity Relationship Diagram

```
┌─────────────┐
│  profiles   │
│─────────────│
│ id (PK)     │◄─────┐
│ email       │      │
│ username    │      │
│ mapos       │      │
│ level       │      │
└─────────────┘      │
       │             │
       │ 1:N         │ 1:N
       ↓             │
┌─────────────┐      │
│   places    │      │
│─────────────│      │
│ id (PK)     │      │
│ name        │      │
│ location    │      │
│ status      │      │
│ submitted_by│──────┘
└─────────────┘
       │
       │ 1:N
       ↓
┌─────────────┐
│   reviews   │
│─────────────│
│ id (PK)     │
│ place_id(FK)│
│ user_id(FK) │
│ rating      │
└─────────────┘

┌─────────────┐
│ connections │
│─────────────│
│ id (PK)     │
│ user1_id(FK)│──┐
│ user2_id(FK)│  │
│ status      │  │
└─────────────┘  │
       │         │
       │ 1:N     │ 2:1
       ↓         │
┌─────────────┐  │
│  messages   │  │
│─────────────│  │
│ id (PK)     │  │
│ connection  │  │
│ sender_id   │──┘
│ content     │
└─────────────┘

┌─────────────┐
│transactions │
│─────────────│
│ id (PK)     │
│ user_id(FK) │──┐
│ amount      │  │
│ type        │  │
└─────────────┘  │
                 │ N:1
┌─────────────┐  │
│achievements │  │
│─────────────│  │
│ id (PK)     │  │
│ user_id(FK) │──┘
│ badge_name  │
└─────────────┘
```

### Table Relationships

| Table | Relationships |
|-------|--------------|
| profiles | → places (1:N), reviews (1:N), connections (1:N), messages (1:N), transactions (1:N), achievements (1:N) |
| places | → reviews (1:N), saved_places (1:N) |
| connections | → messages (1:N) |
| profiles | ↔ profiles (connections M:N) |

## 🔐 Security Architecture

### Authentication Layer

```
┌─────────────────────────────────────┐
│         Client Request              │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Middleware                  │
│  - Check Session                    │
│  - Verify JWT Token                 │
│  - Route Protection                 │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Supabase Auth               │
│  - Validate Token                   │
│  - Check Expiry                     │
│  - Refresh if Needed                │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         RLS Policies                │
│  - Check User ID                    │
│  - Verify Permissions               │
│  - Filter Data                      │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Database Access             │
└─────────────────────────────────────┘
```

### RLS Policy Structure

```
Policy: "Users can view own profile"
├── Table: profiles
├── Operation: SELECT
├── Using: auth.uid() = id
└── Check: TRUE

Policy: "Users can update own profile"
├── Table: profiles
├── Operation: UPDATE
├── Using: auth.uid() = id
└── Check: TRUE

Policy: "Admins can manage all"
├── Table: *
├── Operation: ALL
├── Using: EXISTS (
│     SELECT 1 FROM profiles
│     WHERE id = auth.uid()
│     AND role = 'admin'
│   )
└── Check: TRUE
```

## 🎨 Component Architecture

### Component Hierarchy

```
App
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   ├── Navigation
│   │   └── UserMenu
│   ├── Main Content
│   │   └── Page Components
│   └── Footer
│
├── Pages
│   ├── Dashboard
│   │   ├── InteractiveMap
│   │   │   ├── UserMarker
│   │   │   └── MapControls
│   │   ├── PlaceList
│   │   │   └── PlaceCard
│   │   └── Sidebar
│   │
│   ├── Places
│   │   ├── SearchBar
│   │   ├── FilterBar
│   │   ├── PlaceGrid
│   │   │   └── PlaceCard
│   │   └── PlaceModal
│   │
│   ├── Chat
│   │   ├── ConnectionList
│   │   ├── ChatWindow
│   │   │   ├── MessageList
│   │   │   └── MessageInput
│   │   └── UserInfo
│   │
│   ├── Profile
│   │   ├── ProfileHeader
│   │   ├── StatsCards
│   │   ├── AchievementGrid
│   │   └── Leaderboard
│   │
│   └── Admin
│       ├── AdminDashboard
│       ├── UserManagement
│       └── PlaceApproval
│
└── Shared Components
    ├── UI
    │   ├── Button
    │   ├── Input
    │   ├── Modal
    │   └── Card
    ├── Modals
    │   ├── ConnectModal
    │   ├── PlaceModal
    │   └── GameModal
    └── Games
        └── MiniGame
```

### State Management

```
┌─────────────────────────────────────┐
│         Global State (Zustand)      │
│  ┌──────────────────────────────┐  │
│  │  useAuthStore                │  │
│  │  - user                      │  │
│  │  - setUser                   │  │
│  │  - updateMapos               │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│      Component Local State          │
│  ┌──────────────────────────────┐  │
│  │  useState                    │  │
│  │  - UI state                  │  │
│  │  - Form data                 │  │
│  │  - Loading states            │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Server State                │
│  ┌──────────────────────────────┐  │
│  │  Supabase Queries            │  │
│  │  - Fetched data              │  │
│  │  - Cached data               │  │
│  │  - Realtime subscriptions    │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 🔄 Real-time Architecture

### Realtime Subscription Flow

```
Client Subscribes
        ↓
Supabase Realtime Server
        ↓
PostgreSQL Replication
        ↓
Change Detected
        ↓
Event Published
        ↓
WebSocket Message
        ↓
Client Receives Update
        ↓
UI Updates
```

### Channel Structure

```
messages:connection_id
├── Event: INSERT
├── Schema: public
├── Table: messages
├── Filter: connection_id=eq.{id}
└── Callback: handleNewMessage()

connections:user_id
├── Event: UPDATE
├── Schema: public
├── Table: connections
├── Filter: user1_id=eq.{id} OR user2_id=eq.{id}
└── Callback: handleConnectionUpdate()
```

## 📱 Mobile Architecture

### Responsive Breakpoints

```
Mobile (320px - 767px)
├── Single column layout
├── Bottom navigation
├── Touch-optimized buttons
└── Simplified UI

Tablet (768px - 1023px)
├── Two column layout
├── Side navigation
├── Medium-sized components
└── Hybrid interactions

Desktop (1024px+)
├── Multi-column layout
├── Top navigation
├── Full-featured UI
└── Mouse-optimized
```

## 🚀 Deployment Architecture

### Production Setup

```
┌─────────────────────────────────────┐
│            Vercel Edge              │
│  ┌──────────────────────────────┐  │
│  │  CDN (Static Assets)         │  │
│  │  - Images                    │  │
│  │  - CSS/JS                    │  │
│  │  - Fonts                     │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Vercel Functions            │
│  ┌──────────────────────────────┐  │
│  │  Next.js Server              │  │
│  │  - SSR                       │  │
│  │  - API Routes                │  │
│  │  - Middleware                │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Supabase Cloud              │
│  ┌──────────────────────────────┐  │
│  │  Database (PostgreSQL)       │  │
│  │  Auth Service                │  │
│  │  Storage                     │  │
│  │  Realtime                    │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 🔍 Monitoring Architecture

### Observability Stack

```
Application
    ↓
┌─────────────────────────────────────┐
│         Logging                     │
│  - Console logs                     │
│  - Error logs                       │
│  - Access logs                      │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│         Metrics                     │
│  - Response times                   │
│  - Error rates                      │
│  - User actions                     │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│         Analytics                   │
│  - User behavior                    │
│  - Feature usage                    │
│  - Conversion rates                 │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│         Dashboards                  │
│  - Vercel Analytics                 │
│  - Supabase Dashboard               │
│  - Custom Dashboards                │
└─────────────────────────────────────┘
```

## 🎯 Performance Architecture

### Optimization Layers

```
┌─────────────────────────────────────┐
│         Client-Side                 │
│  - Code splitting                   │
│  - Lazy loading                     │
│  - Image optimization               │
│  - Caching                          │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Network                     │
│  - CDN                              │
│  - Compression                      │
│  - HTTP/2                           │
│  - Prefetching                      │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Server-Side                 │
│  - SSR                              │
│  - ISR                              │
│  - API optimization                 │
│  - Edge functions                   │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Database                    │
│  - Indexes                          │
│  - Connection pooling               │
│  - Query optimization               │
│  - Caching                          │
└─────────────────────────────────────┘
```

## 📊 Scalability Architecture

### Horizontal Scaling

```
Load Balancer
    ↓
┌─────────┐  ┌─────────┐  ┌─────────┐
│ Server 1│  │ Server 2│  │ Server 3│
└─────────┘  └─────────┘  └─────────┘
    ↓            ↓            ↓
┌─────────────────────────────────────┐
│         Shared Database             │
└─────────────────────────────────────┘
```

### Vertical Scaling

```
┌─────────────────────────────────────┐
│         Application                 │
│  - Optimize code                    │
│  - Reduce bundle size               │
│  - Improve algorithms               │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│         Database                    │
│  - Upgrade instance                 │
│  - Add read replicas                │
│  - Optimize queries                 │
└─────────────────────────────────────┘
```

## 🎨 Design System Architecture

### Component Library Structure

```
Design Tokens
├── Colors
│   ├── Primary (Purple-Blue)
│   ├── Secondary (White)
│   └── Semantic (Success, Error, etc.)
├── Typography
│   ├── Font Family (Inter)
│   ├── Font Sizes
│   └── Font Weights
├── Spacing
│   └── Scale (4px base)
└── Shadows
    └── Elevation levels

Base Components
├── Button
├── Input
├── Modal
└── Card

Composite Components
├── PlaceCard
├── UserMarker
├── ChatWindow
└── Leaderboard

Page Templates
├── Dashboard
├── Profile
└── Admin
```

---

## 📚 Architecture Principles

### 1. Separation of Concerns
- UI components separate from business logic
- API layer separate from data layer
- Clear boundaries between modules

### 2. Scalability
- Horizontal scaling ready
- Database optimized
- CDN for static assets
- Edge functions for global performance

### 3. Security
- Authentication at every layer
- RLS policies on database
- Input validation
- HTTPS everywhere

### 4. Performance
- Code splitting
- Lazy loading
- Caching strategies
- Optimized queries

### 5. Maintainability
- TypeScript for type safety
- Modular architecture
- Clear naming conventions
- Comprehensive documentation

---

**This architecture supports the current features and is designed to scale as MapMates grows!**
